% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/advanced.R
\name{mdb_prop}
\alias{mdb_prop}
\title{Get Properties List From MDB Database}
\usage{
mdb_prop(
  path,
  name = NULL,
  propcol = "LvProp",
  version = FALSE,
  as_list = TRUE
)
}
\arguments{
\item{path}{Path to \code{.mdb}/\code{.accdb} file.}

\item{name}{Object name (\code{table}, \code{query}, etc.); may be a character vector.}

\item{propcol}{Property column name. Defaults to \code{LvProp}.}

\item{version}{Logical; when \code{TRUE}, return mdbtools version.}

\item{as_list}{Logical; defaults to \code{TRUE}. When \code{TRUE}, wraps the result in
an \code{mdblist} object so the dedicated print method is used.}
}
\value{
A named list with one entry per element of \code{name}. Each entry is
itself a named list of named character vectors, one per property block
(e.g. \code{"(none)"} for table-level properties, or a column/field name).
Access individual values with \code{p[["Orders"]][["(none)"]]["Description"]}.
}
\description{
\code{mdb-prop} retrieves properties for one or more objects in an MDB database.
\code{name} is the name of the table, query, or other object.
\code{propcol} is the name of the \code{MSysObjects} column containing properties and
defaults to \code{LvProp}.
}
\examples{
db <- mdbr:::.mdb_example_nwind_path()
if (nzchar(db)) {
  p <- mdb_prop(db, "Orders")
  p[["Orders"]][["(none)"]]["Description"]
  p2 <- mdb_prop(db, c("Orders", "Orders Qry"))
}
}
